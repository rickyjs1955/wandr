# Production Environment Variables Example
# Copy this file to .env.prod and fill in the values
# NEVER commit .env.prod to version control

# ==========================================
# Application Settings
# ==========================================
ENVIRONMENT=production
VERSION=1.0.0
LOG_LEVEL=info

# ==========================================
# Security
# ==========================================
# REQUIRED: Generate a strong secret key (256-bit recommended)
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=CHANGE_ME_TO_A_STRONG_RANDOM_STRING

# ==========================================
# Database (PostgreSQL)
# ==========================================
POSTGRES_DB=spatial_intel
POSTGRES_USER=spatial_user
# REQUIRED: Use a strong password
POSTGRES_PASSWORD=CHANGE_ME_TO_A_STRONG_PASSWORD

# ==========================================
# Redis (Session Store)
# ==========================================
# REQUIRED: Use a strong password
REDIS_PASSWORD=CHANGE_ME_TO_A_STRONG_PASSWORD

# ==========================================
# Object Storage (MinIO/S3)
# ==========================================
# REQUIRED: Use strong credentials
MINIO_ROOT_USER=CHANGE_ME_TO_A_STRONG_USERNAME
MINIO_ROOT_PASSWORD=CHANGE_ME_TO_A_STRONG_PASSWORD

# ==========================================
# CORS Configuration
# ==========================================
# Comma-separated list of allowed origins
# Production example: https://yourdomain.com,https://www.yourdomain.com
CORS_ORIGINS=https://yourdomain.com

# ==========================================
# Deployment Notes
# ==========================================
# 1. All passwords should be at least 16 characters with mixed case, numbers, and symbols
# 2. SECRET_KEY should be unique per environment (staging vs production)
# 3. Update CORS_ORIGINS to match your actual domain
# 4. For production, consider using a secrets manager (AWS Secrets Manager, HashiCorp Vault, etc.)
# 5. Rotate all credentials regularly (at least every 90 days)
